Проект веб-сайт
написан на python, flask, bs4, sql, html, css
Структура проекта:
WebsiteFlask (название проекта)
    data (файлы связанные с базой данных)
        filling_db (файлы заполняющие базу данных комплектующих)
            data_files (файлы, нужные для заполнения)
            all.py (заполняет все таблицы комплектующих)
    db (хранит базу данных)
    forms (файлы для создания пользовательский форм заполнения данных)
    static (хранит статичные (неизменяемые) данные)
        css (стили для html)
        images (фото, преимущественно комплектующих)
    templates (html-ки)
    main.py (запускает сайт)
    make DB.py (создает базу данных)
    README.md (файл для ознакомления с проектом)